// tests/app.test.ts

import request from 'supertest';
// import app from '../src/app'; // Your Express app
// import { db } from '../src/services'; // Knex instance

describe('API Tests', () => {
  // beforeAll(async () => {
    // Seed the database before running the tests
    // await db('users').insert([
    //   { name: 'John Doe', email: 'johndoe@example.kz' },
    //   { name: 'Jane Doe', email: 'johndoe2@example.kz' },
    // ]);
  // });

  // afterAll(async () => {
    // Cleanup after tests
    // await db('users').del();
    // await db.destroy(); // Close the database connection
  // });

  // it('should return all users', async () => {
  //   const response = await request(app).get('/api/user/users');
  //   expect(response.status).toBe(200);
  //   expect(response.body).toHaveLength(2);
  // });
});
