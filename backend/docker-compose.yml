services:
  app:
    build: .
    container_name: procedural-documents-backend
    env_file:
      - .env
    ports:
      - "3001:3001"
    networks:
      - app-network
    command: sh -c "npm start"

networks:
  app-network:
    driver: bridge
